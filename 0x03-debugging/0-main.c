#include "main.h"

/**
 * main - main to test a function that check if number positive or negative
 * Return: Always 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
